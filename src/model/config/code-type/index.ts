export interface CodeTypeReqModel {
    code: string;
    name: string;
    description: string;
}

export interface CodeTypeModel {
    id: number;
    code: string;
    name: string;
    description: string;
}