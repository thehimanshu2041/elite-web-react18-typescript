export interface CodeReqModel {
    code: string;
    name: string;
    description: string;
    codeTypeId: number;
}

export interface CodeModel {
    id: number;
    code: string;
    name: string;
    description: string;
    codeTypeId: number;
}
